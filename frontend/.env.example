# Base URL for backend API (no trailing slash)
# Development options:
# - If not set, Vite proxy maps /api -> http://localhost:8000
# - Or set explicitly to bypass proxy:
VITE_API_URL=http://localhost:8000

# Production example:
# VITE_API_URL=https://your-backend-domain